<div class="relative">

    <button type="button" class="absolute top-0 right-0 flex h-full items-center pr-2">
    </button>

    <input 
        type="{{ $type }}" 
        placeholder="{{ $placeholder }}"
        name="{{ $name }}"
        value="{{ $value }}"
        id="{{ $name }}"
        class="w-full pr-8 rounded-sm border-0 py-1.5 px-2.5 text-sm ring-1 ring-slate-300 placeholder:text-slate-400 focus:ring-2"
    >
</div>